git clone git@hio.fr:/home/git/vim ~/.vim

ln -s ~/.vim/dotvim ~/.vimrc

cd ~/.vim
git submodule init
git submodule update
git submodule foreach git submodule init
git submodule foreach git submodule update
